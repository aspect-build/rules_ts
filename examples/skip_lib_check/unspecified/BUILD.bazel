load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "unspecified",
    srcs = ["source.ts"],
    declaration = True,
    # ts_project will not set the skipLibCheck flag when skip_lib_check is set to None
    skip_lib_check = None,
    deps = [
        "//examples:node_modules/@types/node",
    ],
)
