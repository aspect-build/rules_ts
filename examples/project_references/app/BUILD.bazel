load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "compile",
    composite = True,
    declaration = True,
    extends = "//project_references:tsconfig-base",
    deps = ["//project_references/lib_b"],
)
