Design

-   Only support connect-es/protobuf-es currently. We'll allow other protoc plugins when some protobuf rules understand them?
-   Only support .js/.d.ts emit pair, as we don't wire up with a ts_project transpiler right now.
