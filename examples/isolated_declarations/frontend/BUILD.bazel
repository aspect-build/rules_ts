load("@aspect_rules_ts//ts:defs.bzl", "ts_project")

ts_project(
    name = "frontend",
    # Use a dictionary to verify the logic that copies attribute values
    tsconfig = {
        "compilerOptions": {
            "declaration": True,
            "isolatedDeclarations": True,
        },
    },
    deps = ["//examples/isolated_declarations/core"],
)
