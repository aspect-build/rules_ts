load("@aspect_rules_ts//ts:defs.bzl", "ts_config", "ts_project")
load("@bazel_skylib//rules:build_test.bzl", "build_test")

ts_config(
    name = "tsconfig",
    src = "tsconfig.json",
)

# Covers the tsc emit action (mnemonic TsProject) and the options validation
# action (mnemonic TsValidateOptions).
ts_project(
    name = "lib",
    srcs = ["lib.ts"],
    declaration = True,
    source_map = True,
    tsconfig = ":tsconfig",
)

# Covers the isolated typecheck action (mnemonic TsProjectCheck) and the emit
# action in its isolated form (mnemonic TsProjectEmit).
ts_project(
    name = "lib_isolated_typecheck",
    srcs = ["lib.ts"],
    declaration = True,
    isolated_typecheck = True,
    out_dir = "isolated_typecheck",
    source_map = True,
    tsconfig = ":tsconfig",
)

# Gives the `bazel test //...` step in CI something to run; the actual test
# assertions live in test.sh, which is run as a separate CI step.
build_test(
    name = "test",
    targets = [
        ":lib",
        ":lib_types",
        ":lib_isolated_typecheck",
        ":lib_isolated_typecheck_types",
    ],
)
